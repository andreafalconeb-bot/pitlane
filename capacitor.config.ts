import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.mad4performance.pitlane',
  appName: 'PITLANE',
  webDir: 'dist',
  backgroundColor: '#0a0c10',
}

export default config
